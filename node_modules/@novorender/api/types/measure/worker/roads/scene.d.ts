import { Downloader } from "../util";
import type { CrossSections } from "./cross_section";
import type { CrossSlope, RoadCrossSection, RoadProfiles } from "../..";
export declare class RoadTool {
    readonly baseUrl: URL;
    data: Map<string, CrossSections | null>;
    readonly downloader: Downloader;
    constructor(baseUrl: URL);
    findShoulderIndex(codes: number[]): {
        leftShoulder: number;
        rightShoulder: number;
    };
    downloadSections(name: string): Promise<CrossSections | null>;
    private getCrossSections;
    getCrossSection(name: string, param: number): Promise<RoadCrossSection | undefined>;
    getRoadProfiles(name: string): Promise<RoadProfiles | undefined>;
    getCrossSlope(name: string): Promise<CrossSlope | undefined>;
}
//# sourceMappingURL=scene.d.ts.map