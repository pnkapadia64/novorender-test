import { mat3, mat4, type ReadonlyMat4, vec3 } from "gl-matrix";
import { Ray } from "./ray";
export interface CameraData {
    readonly viewWorldMatrix: ReadonlyMat4;
    readonly pixelSize: number;
}
export declare class Camera {
    readonly viewWorldMatrix: ReadonlyMat4;
    readonly viewWorldMatrixNormal: mat3;
    readonly worldViewMatrix: mat4;
    readonly pixelSize: number;
    constructor(data: CameraData);
    get data(): CameraData;
    get backward(): vec3;
    get scaleTransform(): DOMMatrix;
    emitRay(x: number, y: number): Ray;
}
//# sourceMappingURL=camera.d.ts.map