import type { CollisionValues } from ".";
import type { MeasureSettings, MeasureView, MeasureWorker, ParametricEntity } from "../../measure_view";
/**
 * Module for all collision calculations
 */
export declare class CollisionModule {
    readonly worker: MeasureWorker;
    readonly parent: MeasureView;
    constructor(worker: MeasureWorker, parent: MeasureView);
    /**
     * Returns collision values between 2 entities
     * currently only works for two cylinders
     */
    collision(a: ParametricEntity, b: ParametricEntity, setting?: MeasureSettings): Promise<CollisionValues | undefined>;
}
//# sourceMappingURL=module.d.ts.map